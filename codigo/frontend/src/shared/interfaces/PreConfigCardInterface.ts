export interface PreConfigCardProps {
  id: string;
  name: string;
  image: string;
  description: string;
}

export interface Plugin {
  id: string;
  name: string;
  image: string;
  description: string;
}

export interface selectedObjectProps {
  id: string;
  selected: boolean;
}
